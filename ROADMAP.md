# خارطة التوسّع — أوراقنا v2

> مقترح، لم يُعتمد بعد. حُرِّر 2026-08-22 بعد إطلاق v1.0.
> `PROJECT_MAP.md` يبقى مرجع الحالة الفعلية؛ هذا الملف مرجع النية.

## الوضع الحالي

مجال واحد (رياضيات) · ٣ مهارات · صفحة واحدة · مُطلَق ويعمل على `awraqna.com`.

## الهدف

ثمانية مجالات، لكل مجال صفحته المستقلة، وصفحة رئيسية تجمعها —
على غرار بنية `007.gallery/services/`.

**السبب الحقيقي ليس التنظيم بل SEO:** صفحة واحدة تنافس على استعلام واحد.
ثماني صفحات تنافس على ثمانية عناقيد استعلامات مستقلة، وكل واحدة تُفهرَس
وتُشارَك وتُربَط منفردة. هذا هو ما يحوّل الموقع إلى «ساقية جارية».

---

## القيد الحاكم — والاصطدام به

`CLAUDE.md`: **المحتوى يُولَّد بالخوارزمية. صفر بنك أسئلة. صفر أصول مرسومة.**

طلب «صور حيوانات ورسوم للأطفال» للتلوين **يخرق هذا القيد مباشرة** —
لأن صورة الأسد لا تُولَّد بخوارزمية، بل تُرسم مرة وتُخزَّن.

ثلاثة مسارات، والقرار لصاحب المشروع:

| المسار | الكلفة | الأثر على المبدأ |
|---|---|---|
| **أ. أشكال بارامترية بحتة** — ماندالا، تبليط، زخرفة گيري، منحنيات superformula (زهور، أصداف، نجوم) | صفر أصول · كود فقط | سليم تماماً |
| **ب. أصول CC0 خارجية** — مكتبات SVG مفتوحة | بحث + تدقيق رخص | يخرق «صفر أصول» لكن لا يخرق «صفر عمل يدوي متكرر» |
| **ج. رسم يدوي** | ساعات رسم لكل صورة، بلا نهاية | يقتل سبب اختيار المشروع أصلاً |

**التوصية: (أ) الآن، و(ب) لاحقاً إن ثبت الطلب.** المسار (أ) يعطي تلويناً
لا نهائياً وهوية بصرية عربية مميزة (الزخرفة الهندسية الإسلامية خوارزمية بطبعها)،
ولا ينافس فيه أحد عربياً.

---

## تحذير تجاري — جمهور الأطفال

سبب اختيار المشروع كان: **الجمهور بالغ (أمهات ومعلمون)، فقيود إعلانات
محتوى الأطفال لا تنطبق.** إضافة «تلوين للأطفال» قد تُصنِّف صفحات بعينها
كـ child-directed لدى AdSense ⇒ إعلانات غير مخصصة وعائد أقل بكثير.

**التخفيف:** الصياغة والواجهة تخاطب الأهل والمعلم دائماً
(«اطبع لطفلك» لا «لوّن الآن»)، والتصنيف يبقى على مستوى الصفحة لا الموقع.
راجع سياسة AdSense قبل إطلاق مجال الفنون تحديداً.

---

## أثر معماري — معيار «٣ نقرات»

القرار المحسوم رقم ١ في `PROJECT_MAP.md` كان: **صفحة واحدة، لا صفحة وسيطة.**
الصفحة الرئيسية تنقضه وتضيف نقرة.

**إعادة التعريف المقترحة:** المعيار يُقاس **من صفحة الأداة، لا من الجذر**.
- كل صفحة أداة تُفهرَس وتُشارَك مباشرة ⇒ الزائر من جوجل يهبط على الأداة لا على الجذر.
- كل صفحة أداة تفتح بورقة جاهزة للطباعة فوراً (كما هي الآن).
- الصفحة الرئيسية للاكتشاف والزائر الأول فقط، لا لمسار الاستخدام المتكرر.

---

## البنية المقترحة

```
awraqna.com/
├── index.html              الرئيسية — بطاقات المجالات
├── math/index.html         الرياضيات
├── art/index.html          الفنون والرسم
├── coding/index.html       البرمجة
├── language/index.html     اللغات والحروف
├── puzzles/index.html      الألغاز والمنطق
├── teacher/index.html      أدوات المعلّم
├── science/index.html      العلوم والقياس
├── time-money/index.html   الوقت والمال
└── assets/
    ├── brand.css
    ├── sheet.css           قالب الطباعة المشترك — يُستخدم ٨ مرات
    ├── core.js             rng · i18n · state · print · بناء الأزرار
    └── gen/
        ├── math.js         (الحالي، مُوسَّع)
        ├── art.js
        ├── coding.js
        └── ...
```

**`core.js` إلزامي قبل المجال الثالث.** المنطق المشترك اليوم مبعثر في `app.js`
(mulberry32 · buildChips · setLang · save/load · print). تكراره ٨ مرات
كارثة صيانة. قاعدة الـ٣ مرات تتحقق عند المجال الثالث — استخرجه هناك، لا قبل ولا بعد.

---

## المجالات الثمانية

### ١. الرياضيات (توسيع الموجود)
قسمة (بباقٍ وبدونه) · كسور (تبسيط، جمع، تمثيل بصري SVG) · مقارنة `> < =`
· أنماط ومتسلسلات · جدول الضرب التفاعلي + ورقة امتحان · مربعات سحرية
· أهرام الجمع · ترتيب الأعداد.
كلها خوارزمية بحتة. **أرخص توسّع وأعلى عائد فوري.**

### ٢. الفنون والرسم
- **توصيل النقاط**: من منحنيات بارامترية (superformula · epicycloid · حلزونات
  · نجوم منتظمة) ⇒ زهور وأصداف ونجوم بلا أصل مرسوم واحد.
  الخوارزمية: ولّد المسار → أخذ عيّنات N نقطة → رقّمها.
- **التلوين**: ماندالا شعاعية · تبليط هندسي · **زخرفة گيري إسلامية**
  (خوارزمية بطبعها، وهوية بصرية لا ينافسنا فيها أحد عربياً).
- **متاهات** بمستويات (recursive backtracker).
- **الرسم بالشبكة**: انسخ الشكل مربعاً بمربع.
- **التناظر**: أكمل النصف الآخر.

### ٣. البرمجة
جداول تتبّع (trace tables) لحلقات بسيطة · تحويل أنظمة العد (2/8/10/16)
· جداول الحقيقة المنطقية · **الروبوت على الشبكة** (تعليمات → أين ينتهي؟ —
برمجة بلا حاسوب) · ترتيب خطوات خوارزمية · أنماط متسلسلات.

### ٤. اللغات والحروف
- **تتبّع الحروف العربية بأشكالها الأربعة** (منفصل/أول/وسط/آخر) —
  عبر `-webkit-text-stroke` على خط النظام ⇒ حروف مجوّفة للتتبّع **بلا ملف خط
  وبلا أصل مرسوم**. هذه أنظف فكرة في المشروع كله.
- تتبّع الأرقام العربية والهندية (٠-٩ و 0-9).
- ورقة تلوين الحروف.
- ترتيب حروف كلمة / كلمات متقاطعة — تحتاج **معجم** (قائمة كلمات).
  المعجم ليس بنك أسئلة: منه تُولَّد تمارين لا نهائية. حدٌّ مقبول.

### ٥. الألغاز والمنطق
سودوكو (٤×٤ · ٦×٦ · ٩×٩) · متاهات · نونوغرام · كاكورو · كِنكِن
· بحث عن الكلمات (من المعجم).
**خوارزمية ١٠٠٪ وطلب البحث عليها ضخم.** أعلى نسبة عائد/جهد في القائمة كلها.

### ٦. أدوات المعلّم
ورق مسطر/نقطي/شبكي (عربي وإنجليزي) · بطاقات أسماء · شهادات تقدير
· جدول حصص · كشف حضور · عجلة اختيار عشوائي · تقسيم مجموعات.
**الأسهل تقنياً على الإطلاق** وبعضه (ورق الرسم البياني) من أعلى الاستعلامات طلباً عالمياً.

### ٧. العلوم والقياس
ورق رسم بياني (مربعات · متساوي القياس · قطبي · لوغاريتمي) · تحويل وحدات
· شبكة إحداثيات · مسائل سرعة/كثافة بقوالب.

### ٨. الوقت والمال
قراءة الساعة (عقارب SVG مولّدة) · حساب المدة · تمارين العملات العربية
· تقويم شهري فارغ.

---

## الترتيب المقترح — لا تُنفَّذ الثمانية دفعة واحدة

| الموجة | المجالات | لماذا |
|---|---|---|
| **أ** | أدوات المعلّم · الألغاز · توسيع الرياضيات | أقل جهد، أعلى طلب بحثي، خوارزمي بحت، صفر خلاف على المبدأ |
| **ب** | الفنون · اللغات | جوهر طلب صاحب المشروع، وتحتاج عمل تصميم أكثر |
| **ج** | البرمجة · العلوم · الوقت والمال | جمهور أضيق، تُبنى بعد ثبات البنية |

**استخراج `core.js` يقع بين الموجة أ والموجة ب.**

بعد كل موجة: تحديث `sitemap.xml` وإرسالها في Search Console،
وإضافة الصفحات الجديدة إلى `HOUSE[]` في `ads.js`.

---

## معايير النجاح — قابلة للتشغيل

1. كل صفحة مجال تطبع ورقة A4 نظيفة: `--print-to-pdf` ثم عدّ الصفحات = المتوقع.
2. كل صفحة مجال: `scrollWidth === clientWidth` عند 320px بالعربية والإنجليزية.
3. `sitemap.xml` يحوي كل صفحة مجال، وكلها ترجع `200`.
4. صفر تكرار للمنطق المشترك بعد استخراج `core.js`:
   `grep -c mulberry32 assets/gen/*.js` → `0`.
5. زمن تحميل أي صفحة مجال < ١ ثانية (لا مكتبات، لا CDN).

---

## معلّق فوراً — لا علاقة له بالتوسّع

- `www.awraqna.com` لا يستجيب (`000`). أضف النطاق الفرعي في Cloudflare Pages.
