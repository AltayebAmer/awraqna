# أوراقنا — awraqna.com

**اقرأ `PROJECT_MAP.md` قبل أي تعديل.** بدون ذلك ستكسر شيئاً يعمل.
وحدّث قسم `[ORPHANS & PENDING]` فيه بعد كل مرحلة ناجحة.

## المبدأ الحاكم — لا يُخالَف
المحتوى يُولَّد بالخوارزمية، لا يُكتب باليد.
صفر بنك أسئلة. صفر ملفات صوت. صفر ترجمة محتوى.
أي ميزة تخرق هذا القيد تُرفض.

## قيود تقنية صارمة
- HTML/CSS/JS ثابت. بلا build step، بلا npm، بلا أي تبعية خارجية.
- **لا CDN إطلاقاً.** الخطوط خطوط نظام فقط (`system-ui`, `SF Arabic`, `Geeza Pro`).
- ثنائي اللغة AR/EN عبر سمتَي `data-ar`/`data-en` + `html[lang]`. لا مكتبة i18n.
- لا فيضان أفقي حتى عرض 360px — يُتحقَّق منه بـ
  `document.documentElement.scrollWidth === clientWidth`، لا يُفترض.
- `assets/generators.js` **منطق رياضي نقي** — ممنوع لمس DOM داخله.
  هو المشترك مع المرحلة ٢ (جدول الضرب)، وتلويثه بالواجهة يقتل إعادة الاستخدام.
- `assets/sheet.css` هو الطبقة الوحيدة التي تُطبع. لا تخلط قواعد الطباعة بـ `brand.css`.
- إخفاء الطباعة **بالإقصاء** (`body > *:not(#print-root)`) لا بقائمة `.no-print` يدوية.

## الطباعة
اختبر في Chrome Print Preview فعلياً. "من المفترض أن يعمل" = فشل.
