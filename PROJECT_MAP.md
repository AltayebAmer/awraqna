# PROJECT_MAP — أوراقنا | awraqna.com

> الذاكرة الخارجية للمشروع. اقرأه قبل أي تعديل، وحدّث `[ORPHANS & PENDING]` بعد كل مرحلة.
> آخر تحديث: 2026-08-22

---

## [TECH_STACK]

| الطبقة | الاختيار | الإصدار | السبب |
|---|---|---|---|
| Runtime | متصفح فقط | — | بلا خادم، بلا تكاليف تشغيل |
| Build | **لا شيء** | — | ملفات ثابتة تُفتح مباشرة |
| تبعيات | **صفر** | — | لا npm، لا CDN، لا مكتبة PDF |
| الخطوط العربية | Amiri · Aref Ruqaa · Reem Kufi (OFL) | مجزّأة | 669KB ⇒ 146KB |
| الخطوط الإنجليزية | Caveat · Great Vibes (OFL) | مجزّأة | 840KB ⇒ 91KB |
| PDF | `@media print` أصلي | — | المتصفح هو محرك الطباعة |
| RNG | `mulberry32` مكتوب يدوياً | — | seed صريح ⇒ «إعادة توليد» = `seed++` |
| اختبار | `node -e` على `generators.js` | node ≥ 18 (النظام) | غير مُضمَّن في المشروع، للتطوير فقط |
| خادم محلي | `python3 -m http.server` | مثبّت مع macOS | تطوير فقط |
| الاستضافة | Cloudflare Pages | — | النطاق مُشترى من Cloudflare Registrar |
| الأيقونات | SVG → PNG عبر Chrome headless | — | لا مكتبة صور؛ `favicon.svg` هو المصدر |

**أسرار:** لا يوجد أي سر في المشروع ⇒ لا `.env`. `.gitignore` موجود من البداية.

---

## [SYSTEM_FLOW]

```
UI (select / buttons)
      │ change
      ▼
state = { grade, skill, difficulty, count, seed }
      │
      ▼
generators.generate(skill, state)      ← rng = mulberry32(seed)
      │  ×count · رفض المكرر · قيود الصعوبة
      ▼
questions[] = [{ a, op, b, answer }]   ← مصدر الحقيقة الوحيد
      │
      ├──► renderSheet()   → #sheet    (سؤال + فراغ حل)
      └──► renderAnswers() → #answers  (نفس المصفوفة + الحل)
                                        ⇒ التطابق صحيح بالبناء لا بالاختبار
```

**رحلة المستخدم (٣ نقرات كحد أقصى):**
`فتح الموقع (ورقة جاهزة سلفاً — 0 نقرة)` → `تغيير المهارة` → `تغيير الصعوبة` → `طباعة`
الطباعة المباشرة بلا تعديل = **نقرة واحدة**.

---

## [ARCHITECTURE]

```
awraqna/
├── index.html              الرئيسية — ٩ مجالات، كلها حيّة
├── math/index.html         الرياضيات — ٦ مهارات
├── puzzles/index.html      الألغاز — سودوكو ومتاهات
├── teacher/index.html      أوراق المعلّم — ٤ أنواع ورق
├── art/index.html          الفنون — رسم خطوة بخطوة · تلوين · نقاط · تناظر
├── repeat/index.html       الزخرفة والتكرار — ٧ أشكال × ٣ أوضاع + سماكة وتكرار
├── language/index.html     الحروف — عربية · إنجليزية · أرقام · تلوين · ٤ خطوط
├── coding/index.html       البرمجة — أنظمة عد · تتبّع · منطق · روبوت
├── science/index.html      العلوم — دوال · مسطرة ومنقلة · وحدات · قوانين · إحداثيات
├── time-money/index.html   الوقت والمال — ساعة · مدّة · مال · تقويم
├── 404.html · robots.txt · sitemap.xml · _headers
├── favicon.ico · favicon.svg · apple-touch-icon.png
└── assets/
    ├── brand.css           نظام تصميم 007 (منسوخ)
    ├── page.css            قشرة الصفحة + مبدّل اللغة (شاشة فقط)
    ├── sheet.css           ★ الورقة + @media print — الطبقة المطبوعة الوحيدة
    ├── core.js             ★ النواة: rng · أزرار · لغة · تخزين · طباعة · ترويسة
    ├── page-<domain>.js    ربط كل صفحة بالنواة (إعدادات فقط، بلا منطق)
    ├── gen/<domain>.js     ★ منطق كل مجال — نصوص فقط، ممنوع DOM
    ├── gen/draw.js         ★ وصفات الحيوانات الهندسية (١٤ حيواناً)
    └── ads.js              إعلانات بيتية متبادلة مع 007.gallery

../.claude/launch.json   خادم التطوير المحلي (خارج المجلد، لا يُنشر)
```

### قرارات محسومة — لا يُعاد فتحها
1. ~~**صفحة واحدة، لا صفحة وسيطة.**~~ **نُقض في v2** — انظر القرار ١٢.
2. **`assets/gen/*.js` منطق نقي منفصل — ممنوع DOM داخله.** يعيد نصوصاً فقط،
   فيبقى قابلاً للاختبار في `node` ولإعادة الاستخدام في أي مجال لاحق.
3. **seed صريح.** لا `Math.random()` مباشرة. الـ seed يُعرض على الورقة ⇒ معيار «توليدان مختلفان» قابل للبرهنة.
4. **الإجابات مشتقّة من `questions[]`**، لا مولّدة ثانية.
5. **الصف مستقل عن المهارة.** الصف يضبط مدى الأرقام فقط؛ لا جدول مناهج
   (القرار (أ) — المناهج العربية تختلف، وإخفاء الخيارات يكسر ٣ النقرات).
6. **إخفاء الطباعة بالإقصاء** `body > *:not(#print-root)` ⇒ أي إعلان جديد يختفي بلا صيانة.
7. **ارتفاع `.ansbox` في الطباعة = 30px بالضبط**، و `.vform{line-height:1.15}` إلزامي.
   جُرّب 34px ⇒ ٤٠ سؤالاً تفيض إلى صفحتين. وبلا `line-height` يرث `1.6` من `body`
   فتفيض أيضاً. أي تغيير في هذين الرقمين يتطلب إعادة قياس بعدّ صفحات PDF.
8. **`_headers`: لا `immutable` على `/assets/*`.** بلا build step لا توجد بصمة hash
   في أسماء الملفات، فالتخزين لسنة يعني أن أي إصلاح لن يصل من زار الموقع سابقاً.
   يوم واحد + `must-revalidate`. الأيقونات وحدها تُخزَّن سنة.
9. **ارتفاع ورق المعلّم = مساحة الطباعة ناقص الترويسة ناقص سطر النسبة.**
   أي عنصر يُضاف أسفل الورقة يجب أن يُطرَح من ارتفاع الـSVG وإلا انتقلت لصفحة ثانية.
10. **`.ansbox` صنف مستقل لا وصفي.** كان مقيَّداً بـ `.vform` فاختفت صناديق
   الإجابة في كل مجال لا يستخدم الصيغة العمودية — ظهر في ورقة الساعة أولاً.
11. **ممنوع نص عربي داخل `<text>` في SVG** (تكرار للتأكيد: ظهر مرتين).
12. **مرشِّح جودة الأشكال يُقاس على النقاط النهائية لا على المعادلة.**
   شرط الوضوح وحده (تباعد النقاط) يفضّل الدوائر — أوضح شكل هو أقلّه إثارة.
   لذلك القبول = تباعد ≥1.8 **و** تباين نصف قطر ≥0.30 معاً. حذف أيٍّ منهما
   يعيد المشكلة: إمّا نقاط ملتبسة أو دوائر مملّة.
13. **توزيع النقاط بطول القوس لا بالزاوية.** التوزيع الزاوي يكدّس النقاط حيث
   يصغر نصف القطر فتتلاصق نقطتان ولا يعرف الطفل أيّهما التالية.
14. **`core.js` يملك المشترك، و`gen/*.js` يملك المجال.** أي منطق يتكرر في
   مجالين فأكثر ينتقل إلى النواة فوراً. أي منطق مجال يدخل النواة = خطأ معماري.
15. **ممنوع نص عربي داخل `<text>` في SVG.** جُرّب في ترويسة ورق المعلّم فطُبع
   مبعثراً («e» و «..» بدل الجملة). كل نص عربي يُرسم بـ HTML.
16. **`.puz-svg` بارتفاع 118mm للغزين في الصفحة.** جُرّب 126mm ⇒ أربع صفحات بدل صفحتين.
17. **الصفحة الرئيسية تنقض القرار ١ عمداً** (بقرار صاحب المشروع 2026-08-22).
   معيار «٣ نقرات» يُقاس الآن **من صفحة الأداة لا من الجذر** — كل أداة تُفهرَس
   وتُشارَك مباشرة، والرئيسية للاكتشاف لا لمسار الاستخدام المتكرر.
18. **مبدّل اللغة في نهاية `page.css` عمداً.** تساوي التخصيص يجعل الأخير الفائز؛
   لو وُضع في الأعلى لتغلّب عليه أي `display` لاحق (حدث مع `.badge-soon`).

19. **كل سطر في `404.html` باتجاهه الصريح** (`dir="rtl"` / `dir="ltr"`).
   خلط AR و EN في سطر واحد يقلب موضع النقطة — جُرّب وشوهد وأُصلح.

### عقد `assets/gen/*.js` — مثبَّت
```
render(state) → { title:{ar,en}, sub:{ar,en}, sheet:HTML, answers:HTML, head?:bool }
   ★ يعيد نصوصاً ولا يلمس DOM ⇒ قابل للاختبار في node.

MathGen.generate(skill, { grade, difficulty, count, seed }) → Question[]
skill      : 'add' | 'sub' | 'mul' | 'div' | 'cmp' | 'pat'
difficulty : 'easy' | 'med' | 'hard'
Question   : { a:int, op:'+'|'−'|'×', b:int, answer:int }
```
قيود داخلية: لا ناتج سالب في الصفوف ١–٤ · لا مكرر في الورقة الواحدة ·
لا `a×1` ولا `a+0` في مستوى `hard`.

### نطاق مرفوض عمداً
بنك أسئلة يدوي · ملفات صوت · حسابات/مزامنة · مكتبة PDF · Service Worker (م١) ·
`guardian.js` (خارج نطاق م١) · أي مهارة رابعة قبل الإطلاق.

---

## [ORPHANS & PENDING]

### مُنجَز
- [x] **M0** — الهيكل: مجلد، `.gitignore`، `CLAUDE.md`، `PROJECT_MAP.md`،
      نسخ `brand.css` و `ads.js` من 007.
- [x] **M1** — `assets/generators.js` (add/sub/mul + mulberry32 + عقد `generate`).
      مُتحقَّق: مسح ٥٤ تركيبة (٣ مهارات × ٦ صفوف × ٣ صعوبات) بعدد ٤٠ سؤالاً →
      صفر فشل حسابي، صفر ناتج سالب في الصفوف ١–٤، العدد مضمون في كل تركيبة.
- [x] **M2** — `index.html` + `assets/app.js` + `assets/sheet.css`.
      مُتحقَّق في المتصفح: «إعادة توليد» يبدّل الأسئلة والـ seed،
      `answersMatch: true` بمقارنة كل إجابة بالسؤال المقابل، الإعلان يُركَّب.
- [x] **M3** — الطباعة. مُتحقَّق بـ Chrome headless `--print-to-pdf` وعدّ صفحات PDF فعلي:
      | الحالة | الصفحات |
      |---|---|
      | ١٠ / ٢٠ / ٣٠ / ٤٠ سؤالاً + إجابات | **2** لكلٍّ منها |
      | ٤٠ سؤالاً بلا إجابات | **1** |
      | ٤٠ سؤالاً (صف ٦ ضرب صعب، ٣ خانات) + إجابات | **2** |
      | إنجليزي LTR (صف ٤ طرح صعب، ٤٠ سؤالاً) + إجابات | **2** |
      صفر صفحة زائدة · صفر عنصر واجهة أو إعلان على الورق · الورقة تسع ٤٠ سؤالاً
      في صفحة واحدة دائماً ⇒ لا سؤال ينقسم أصلاً.
- [x] **M4** — RTL/EN وعرض الأجهزة. مُتحقَّق على **٥ مقاسات**: 320 · 390 · 768 · 1024
      (بالعربية والإنجليزية) → `scrollWidth === clientWidth` و **صفر عنصر يتجاوز حدّ العرض**،
      بما في ذلك أسوأ حالة (صف ٦ · ضرب · صعب · ٤٠ سؤالاً بأرقام ٣ خانات).
      الأعمدة تنتقل من ٢ إلى ٤ عند 640px، وصفحة الإجابات من ٣ إلى ٥.
- [x] **M5 (الشِق المحلي)** — التبادل الإعلاني: `awraqna` أُضيف إلى `HOUSE[]` في
      `007-gallery/assets/ads.js`، و `gallery` أُضيف إلى `awraqna/assets/ads.js`.
      مُتحقَّق: `ADS.HOUSE` يعيد ٥ عناصر، صفر خطأ في console، `node --check` نظيف للملفين.
- [x] **M6** — تعديل بناءً على ملاحظات صاحب المشروع (٢٠٢٦-٠٨-٢٢): الورقة كانت نصفها
      فارغاً، ولا مكان مخصص للحل، والأسئلة تتقارب بصرياً.
      • الحد الأقصى ٤٠ سؤالاً (كان ٣٠) والافتراضي أصبح ٤٠ ⇒ الورقة تمتلئ.
      • `.ansbox` — مستطيل صريح لكتابة الإجابة تحت خط العملية.
      • `.q-item` بـ `border-bottom` ⇒ خط فاصل بين صفوف الأسئلة.
      • `.vform{line-height:1.15}` — بدونها يرث `1.6` من `body` فتفيض الـ٤٠ سؤالاً
        إلى صفحة ثانية. **لا تحذف هذا السطر.**
- [x] **M7** — أساسيات SEO والأيقونة: `robots.txt` · `sitemap.xml` · `favicon.ico`
      (٣ مقاسات) · `favicon.svg` · `apple-touch-icon.png` + ٣ وسوم `<link>`.
      مُتحقَّق: الخمسة تُخدَم بـ `200` وبنوع MIME صحيح، الوسوم تُحمَّل من الصفحة بـ `fetch`،
      الـ XML صالح بـ `xml.dom.minidom`، ورأس ICO يُقرأ `type 1 · 3 images`.
- [x] **M8** — حزمة النشر: `_headers` · `404.html` · `DEPLOY.md`.
      مُتحقَّق: ١٣ ملفاً منشوراً تُخدَم بـ `200`، وصفحة 404 صحيحة الاتجاه في اللغتين
      (أُصلح فيها انقلاب موضع النقطة الناتج عن خلط AR و EN في سطر واحد).
- [x] **M9** — أرشيف النشر `../awraqna-v1.0.zip` (28KB · ١٣ ملفاً).
      مُتحقَّق **من الأرشيف نفسه لا من المصدر**: فُكّ في مجلد منفصل، خُدم على منفذ آخر،
      كل المسارات `200`، والطباعة منه = **صفحتان**.

- [x] **M10** — التوسّع v2 (2026-08-22): صفحة رئيسية + ٣ مجالات على نواة مشتركة.
      • `assets/core.js` — استُخرج المشترك (rng · أزرار · لغة · تخزين · طباعة · ترويسة)
        و`app.js` القديم حُذف. المنطق المشترك يُكتب مرة واحدة لا ثماني.
      • **الرياضيات**: أُضيفت القسمة والمقارنة والأنماط ⇒ ٦ مهارات.
      • **الألغاز**: سودوكو ٤×٤/٦×٦/٩×٩ **مضمون الحل الوحيد** (يُتحقَّق بالعدّ)،
        ومتاهات بثلاثة أحجام مع مسار الحل.
      • **أوراق المعلّم**: مسطر/شبكي/نقطي/رسم بياني بوحدات **مليمترية حقيقية**.
      مُتحقَّق:
      | الفحص | النتيجة |
      |---|---|
      | سودوكو: عدد الحلول (٩ تركيبات) | **1** لكلٍّ — لا لغز بحلّين |
      | متاهات قابلة للحل (١٨٠ متاهة) | **180/180** |
      | عدّ صفحات PDF (١٨ حالة) | مطابق للمتوقع في كلٍّ |
      | ٣٢٠px على الصفحات الأربع | صفر فيضان |
      | أخطاء console | صفر |
      | طباعة من الأرشيف المفكوك | 2 · 2 · 1 |

- [x] **M11** — الموجة ب (2026-08-22): الفنون والحروف.
      • **الفنون**: توصيل النقاط · ماندالا · زخرفة هندسية · تناظر — كلها من
        معادلات بارامترية (المعادلة الفائقة · منحنى الوردة · نجوم · حلزون).
        **صفر صورة، صفر مسار مرسوم يدوياً** — كما اشترط `CLAUDE.md`.
      • **الحروف**: تتبّع الحروف العربية بأشكالها من **محرك التشكيل نفسه عبر ZWJ**
        لا من جدول مكتوب. الحروف غير المتصلة تعرض شكلين فقط — حقيقة لغوية لا استثناء.
        الحرف المجوَّف من `-webkit-text-stroke` + تعبئة رمادية كاحتياط لو لم يُدعَم.
      مُتحقَّق:
      | الفحص | النتيجة |
      |---|---|
      | توصيل النقاط: نقطتان ملتبستان (<1.8) في ٢٠٠ شكل | **0** |
      | توصيل النقاط: أشكال مسطّحة (تباين<0.30) | **0** |
      | تباين نصف القطر — وسيط ٢٠٠ شكل | **0.85** |
      | عدّ صفحات PDF (١٦ حالة) | مطابق للمتوقع في كلٍّ |
      | ٣٢٠px على الصفحات الست | صفر فيضان |
      | أخطاء console | صفر |

- [x] **M12** — الموجة ج + معالجة ملاحظات صاحب المشروع (2026-08-22).
      **مجالات جديدة:** البرمجة (أنظمة عد · جداول تتبّع · جداول حقيقة · روبوت الشبكة)
      · العلوم (تحويل وحدات · سرعة/كثافة/قوة/شغل · شبكة إحداثيات)
      · الوقت والمال (ساعة تناظرية · مدّة منقضية · مال · تقويم).
      **معالجات:**
      • **الخط**: اختيار نسخ/كوفي/ثلث بعائلات نظام + معامل حجم **مقيس** لكل خط.
        الرقعة مؤجَّلة — العائلة الوحيدة المتاحة ملوّنة ولا تصلح للتتبّع.
      • **سطر التتبّع** يمتلئ إلى حافة الورقة (80 حرفاً + `overflow:hidden`).
      • **«ماندالا» حُذفت** واستُبدلت بـ«شمسة» مبنية من خلايا مغلقة قابلة للتلوين.
      مُتحقَّق:
      | الفحص | النتيجة |
      |---|---|
      | البرمجة: إجابات خاطئة في ٤٨٠٠ تمرين | **0** |
      | العلوم: إجابات خاطئة في ٤٥٠٠ اختبار | **0** |
      | الوقت والمال: خاطئة في ٤٥٠٠ اختبار | **0** |
      | زاوية عقرب الساعات عند 3:30 | **105°** (لا 90° — يتقدّم بالدقائق) |
      | عدّ صفحات PDF (٢٣ حالة) | مطابق للمتوقع |
      | ٣٢٠px على الصفحات التسع | صفر فائض |

- [x] **M13** — خطوط الخط العربي (2026-08-22، بإذن صاحب المشروع).
      نُزّلت من مستودعاتها الرسمية (aliftype/amiri · aliftype/aref-ruqaa · aliftype/reem-kufi)
      وجُزّئت على المحارف العربية بـ `pyftsubset` إلى woff2:
      | الخط | الأصل | بعد التجزئة |
      |---|---|---|
      | Amiri (نسخ) | 427KB | **79KB** |
      | Aref Ruqaa (رقعة) | 118KB | **45KB** |
      | Reem Kufi (كوفي) | 124KB | **22KB** |
      مُتحقَّق: أشكال الحرف الأربعة تعطي **عروضاً مختلفة** في كل خط
      (ب 93 · بـ 19 · ـبـ 24 · ـب 88 في Amiri) ⇒ التشكيل نجا من التجزئة.
      عدّ صفحات PDF لكل خط في وضعَي التتبّع والتلوين: **1** في العشرة.
      الثلث يبقى على خط النظام — لا يوجد خط ثلث حرّ.
- [x] **M14** — تشابك حقيقي في الزخرفة (بطلب صاحب المشروع).
      كل شريط يُرسم بطبقة محو بيضاء تحت حافتيه، وترتيب الرسم هو ما يقرّر
      مَن يمرّ فوق مَن ⇒ لا حساب تقاطعات. الروابط وُسّعت من `u*0.21` إلى
      `u*0.40` لتتقاطع فعلاً مع أشرطة النجوم — بلا التداخل يتلامس الشريطان
      ولا يظهر «فوق/تحت» إطلاقاً (جُرّب وشوهد).
      مُتحقَّق: ١٢ تركيبة فنون × عدّ صفحات PDF → مطابقة، والصفحات التسع بلا انحدار.

- [x] **M15** — طبقة الملكية ALTAYEB GUARDIAN v4.0 + تدقيق نظام «رد المعروف».
      حُقنت في ١٠ صفحات و٢١ ملف CSS/JS، وأُنشئت `humans.txt` و `LICENSE.md`
      و `.well-known/guardian.json`.
      **الطبقة الخامسة (بصمة المخرجات) مُطفأة عمداً**: المشروع لا يُنزّل ملفاً
      واحداً — مخرَجه ورق مطبوع (تحقّقت: صفر `toBlob`/`toDataURL`/`download`).
      **ثلاثة عيوب وُجدت وأُصلحت:**
      • `guardian.config.json` يقرأه الحاقن **ولا يصل إلى المتصفح** ⇒ ظهر اسم
        المشروع «localhost» وبقيت الطبقة الخامسة مفعّلة. الحل `guardian-config.js`.
      • `guardian_verify.py` يعطي ✓ لـ`_headers` لمجرّد وجود أي ترويسة `X-`،
        وترويسات الملكية لم تكن موجودة أصلاً (الحاقن يتخطّى الملف الموجود).
        أُضيفت يدوياً. **لا تثق بهذا الفحص بعينه.**
      • `404.html` بلا `<footer>` ⇒ لم يصلها التوقيع. أُضيف تذييل.
      • وسم الحارس بلا سمات `data-*` ⇒ التدقيق يسقط، والملكية غير مقروءة من
        HTML وحده. أُضيفت السمات كاحتياط مزدوج مع `guardian-config.js`.
      **الإعلانات:** ٣ من ٥ إعلانات بيتية روابطها `#` (مشاريع لم تُطلق) ⇒
      كانت ٦٠٪ من الظهور تقود إلى تبويب فارغ. `ads.js v1.1` يصفّيها،
      وتعود تلقائياً حين يُوضع رابطها.
      مُتحقَّق: `GUARDIAN.selfTest()` → الطبقات كلها `true` · صفر خطأ console ·
      عدّ صفحات PDF للمجالات الثمانية بلا انحدار · ٩ صفحات فيها إعلان حيّ.

- [x] **M16** — ملاحظات صاحب المشروع على صفحة الحروف (2026-08-23).
      • **سطر النسبة على كل ورقة مطبوعة**: `awraqna.com` + اسم المالك، في اللغتين،
        أسفل كل ورقة وكل صفحة إجابات. الورقة تنتقل مطبوعةً من يد إلى يد،
        وهذا السطر هو الوسيلة الوحيدة ليعرف من يمسكها من أين جاءت.
      • **تصحيح تسمية الأرقام**: ٠١٢٣ = «أرقام هندية» بالعربية و Arabic numerals
        بالإنجليزية، و 0123 عكسها تماماً. الخلط يعلّم خطأً تاريخياً شائعاً.
      • **زرّا خطوط منفصلان** (عربية · إنجليزية) يظهران في النسختين،
        والأبجدية هي التي تختار أيّهما يُطبَّق.
      • **خطوط إنجليزية**: حديث (نظام) · يدوي (Caveat) · خطّي (Great Vibes).
      • **إصلاح عطب**: «لكل ورقة» كان يُتجاهَل كلياً في وضع التلوين فيظهر حرف
        واحد دائماً. الآن ١ / ٢ / ٤ خلايا تملأ الورقة.
      • **اسم الحرف نُقل إلى زاوية الخلية** — كان داخل مساحة التلوين فيُلوَّن سهواً.
      **انحدار وُجد وأُصلح:** سطر النسبة دفع ورق المعلّم إلى صفحة ثانية
      (الورقة كانت تملأ 273mm بالضبط). قُلِّص ارتفاع الـSVG بـ9mm.
      مُتحقَّق: ٢٠ حالة تلوين/تتبّع × كل الخطوط → **1 صفحة** لكلٍّ ·
      المجالات الثمانية بلا انحدار · ١٠ صفحات عند 320px بلا فيضان.

- [x] **M17** — مجال «الزخرفة والتكرار» (بطلب صاحب المشروع، على نهج
      Object > Repeat في Illustrator). الزخرفة نُقلت من الفنون إلى مجالها المستقل.
      • **٧ أشكال** (نجمة · بتلة · معيّن · قوس · مثلّث · حلقات · دمعة) تتغيّر
        نسبها مع الـ seed فتُنتج نتائج لا نهائية من النوع نفسه.
      • **٣ أوضاع**: شعاعي (عدد + قطر + زاوية بدء) · شبكي (أفقي + عمودي)
        · مرآوي (خطوط تناظر + قطر). المساطر تحدّث المعاينة أثناء السحب.
      • **إضافة للنواة (v2.2)**: نوع تحكّم `range` وخاصية `showIf` — صفوف
        كل وضع تظهر وحدها، تماماً كلوحة Illustrator.
      **أربعة عيوب وُجدت وأُصلحت أثناء البناء:**
      • معرّف `#motif` ثابت ⇒ `<use>` يلتقط أول عنصر في المستند فتتحوّل كل
        الأشكال إلى نسخة من أوّلها. المعرّف صار فريداً لكل رسم.
      • «القطر» كان يُقاس إلى حافة الشكل فبقي ثقب في المنتصف ولم تُنتَج
        الوردة الكثيفة أبداً. صار يُقاس إلى مركز الشكل ⇒ الصفر يعطي التقاطع.
      • المرآة كانت تزيح رأسياً فتعطي نسختين فوق/تحت لا جناحين. صارت
        انعكاساً أفقياً حول محور رأسي.
      • `.ctl-row[hidden]` — سمة `hidden` وحدها لا تكفي أمام `display:flex`،
        فظهرت مساطر الأوضاع الثلاثة معاً.
      مُتحقَّق: ٦ حالات طباعة (بما فيها أقصى وأدنى كل مسطرة) → **1 صفحة** لكلٍّ ·
      التسعة مجالات بلا انحدار · ١١ صفحة عند 320px بلا فيضان.

- [x] **M18** — «تعلّم الرسم خطوة بخطوة» (2026-08-23، بعد أن أرسل صاحب المشروع
      أمثلة دروس الرسم). **الأمثلة غيّرت الحكم**: الحيوان في دروس الرسم مبنيّ من
      دوائر وأقواس مرتّبة ⇒ يُوصَف هندسياً، لا يحتاج صورة.
      • **٧ حيوانات** (قطة · كتكوت · بطة · سمكة · أرنب · سلحفاة · فيل)،
        كل واحد **وصفة**: قائمة خطوات، كل خطوة عناصر أوّلية بإحداثيات نسبية.
      • النِّسَب تتغيّر مع الـ seed ⇒ الحيوان نفسه بأشكال لا نهائية.
      • وضعان: **خطوات** (السابق رمادي والجديد داكن + لوحة فارغة للتجربة)
        و**تلوين** (الرسم كاملاً بحجم الصفحة).
      مُتحقَّق: ١٤ حالة طباعة (٧ حيوانات × وضعين) → **1 صفحة** لكلٍّ ·
      التسعة مجالات بلا انحدار · فحص بصري للسبعة: كلها تُقرأ كحيواناتها.
      **حدّ صادق:** هذا أسلوب رسم الأطفال بالخطوط البسيطة، لا لوحة واقعية.
      الواقعية والسريالية ما زالتا خارج ما تُنتجه المعادلات.

- [x] **M19** — ملاحظات صاحب المشروع (2026-08-23).
      **التكرار:** مسطرة **سماكة الخط** (0.4–6) + زرّ **زخارف لكل ورقة** (١/٤/٨)
      بنسخ متطابقة تُقصّ إلى بطاقات.
      **الحيوانات:** من ٧ إلى **١٤** (أُضيف: بومة · بطريق · خروف · ضفدع ·
      فراشة · نحلة · حلزون)، وأُعيدت صياغة الخروف والفيل والأرنب.
      **عيبان أُصلحا:**
      • النسخ المتطابقة كانت تتشارك معرّف `<defs>` — تعمل بالمصادفة لأن
        الأشكال متطابقة، وهي مصادفة لا يُبنى عليها. لكل نسخة معرّفها.
      • `grid-auto-rows:1fr` لا تكفي: الـSVG يحتفظ بنسبته المربّعة فيتمدّد
        خارج الصف ⇒ ٨ زخارف صارت صفحتين. الارتفاع يُثبَّت رقمياً لكل تخطيط.
      **الخروف:** كرات الصوف تُركَّز على محيط الجسم العلوي فتصنع حافة غيمية —
      نثرها اعتباطاً أعطى فوضى لا صوفاً.
      مُتحقَّق: ٢٨ حالة رسم (١٤ حيواناً × وضعين) → **1 صفحة** لكلٍّ ·
      ٧ حالات تكرار (سماكة وعدد وأقصى الحالات) → **1** لكلٍّ · تسعة مجالات بلا انحدار.

- [x] **M20** — الشفافية كوسيلة تعليمية (2026-08-23، بطلب صاحب المشروع).
      مسطرة **«الشفافية» 0–85%** في: الزخرفة والتكرار · الفنون · الحروف.
      الغرض ورقة باهتة يُتبَّع فوقها بالقلم — وهو امتداد لفكرة الحروف الشفافة
      إلى الزخرفة والرسم.
      **قرار التنفيذ:** الشفافية تُطبَّق على **محتوى الورقة وحده** (`.sheet-body`)
      لا على الترويسة وسطر النسبة — وإلا اختفى اسم الطالب والتاريخ واسم الموقع.
      القيمة المخزَّنة هي الشفافية نفسها لا معكوسها، ليطابق الرقمُ اسمَ الزرّ.
      حدّ أدنى للعتامة 0.08 يمنع ورقة لا تظهر على الطابعة.
      مُتحقَّق: ٩ حالات طباعة (٣ مجالات × ٠/٤٠/٨٥%) → **1 صفحة** لكلٍّ ·
      قياس سطوع الورقة المطبوعة: 200.1 → 204.4 → 207.3 مع ازدياد الشفافية ·
      الترويسة تبقى عتامتها `1` بينما المحتوى `0.4` عند شفافية 60% ·
      تسعة مجالات بلا انحدار.

- [x] **M21** — أشكال نباتية + تطوير العلوم (2026-08-23).
      **التكرار:** ٦ أشكال نباتية (ورقة · زهرة · وردة · خزامى · غصن · سعفة)
      ⇒ ١٣ شكلاً. الزهرة والوردة أُعيد بناؤهما: المنحنيات المكعّبة أعطت
      أشواكاً لا بتلات، والأقواس متغيّرة نصف القطر شوّهت الوردة.
      **العلوم:** أداتان بصريتان بمستوى صفحة التكرار:
      • **راسم الدوال** — خطية · تربيعية · تكعيبية · جيبية · عكسية، بثلاثة
        مستويات مدى. المنحنى يُحسب من الدالة نقطةً نقطة ⇒ صحيح بالبناء،
        ويُقطَع عند الانقطاعات (1/x) فلا يُرسم خطٌّ وهمي عبر المحور.
      • **مسطرة ومنقلة للطباعة** — بوحدات mm صريحة في الـSVG ⇒ تخرج
        بمقاسها الحقيقي. **ممنوع `width:100%` عليهما** وإلا فقدتا معناهما.
      **عيبان أُصلحا:** أرقام السنتيمتر والبوصة كانت على الارتفاع نفسه
      فتراكبت · الأرقام السالبة تُعرض «6−» في سياق RTL، وعلامة LRM تثبّتها.
      مُتحقَّق: ١٠ حالات رسم دوال + الأدوات + النباتيات الستّ في الأوضاع
      الثلاثة → عدد الصفحات مطابق · تسعة مجالات بلا انحدار.

- [x] **M22** — أدوات الحلزون والدوّامة (2026-08-23، بمراجع بصرية من صاحب المشروع).
      • **حلزون** بمعاملات Spiral Tool الأربعة نفسها: نصف قطر · اضمحلال ·
        قطاعات · اتجاه — حلزون لوغاريتمي (نصف القطر × معامل عند كل ربع دورة).
        وخيار **أوراق** يحوّله غصناً متسلّقاً.
      • **دوّامة** — شفرات حلزونية عريضة عند القلب مدبَّبة عند الحافة،
        بمعاملات: عدد الشفرات · زاوية اللف · الفراغ الداخلي.
      • **نقاط حلزونية** — توزيع فيلوتاكسي بالزاوية الذهبية 137.5°.
      • **تعبئة**: الدوّامة والنقاط مصمتتان افتراضاً (بلا تعبئة تفقد أثرها)
        مع خيار التفريغ لورقة تلوين.
      **ثلاثة عيوب أُصلحت:** أوراق الحلزون كانت مماسّية فتعبر الحلزون نفسه
      (صارت شعاعية للخارج) · أطراف شفرات الدوّامة مضطربة (أُعيدت هندسة
      الحافة الخلفية: الانفراج يتّسع نحو القلب لا نحو الحافة) · النقاط
      تلتحم عند الكثافة العالية فتصير قرصاً أسود (حجمها ∝ ‎1/√العدد).
      مُتحقَّق: ٧ حالات طباعة شاملة أقصى كل مسطرة → **1 صفحة** لكلٍّ ·
      تسعة مجالات بلا انحدار.

- [x] **M23** — تصحيح أداة الحلزون (2026-08-23، بتصويب صاحب المشروع).
      **الاضمحلال يُقاس لكل لفّة كاملة لا لكل قطاع** كما في Adobe (اللفّة = ٤
      قطاعات). كان يُطبَّق لكل قطاع فصارت 80% تساوي 0.8⁴ ≈ 41% للفّة،
      فخرج الحلزون أضيق بكثير من أداة Illustrator.
      مُقاس بعد الإصلاح: **79.9% بعد لفّة · 64.1% بعد لفّتين** (0.8² = 64%).
      **وعطب معرّف `<defs>`:** رسمان بنفس الإعدادات على صفحة واحدة كانا
      يتشاركان المعرّف فيلتقط `<use>` أوّلهما ويظهران متطابقين. عدّاد داخلي
      يضمن التفرّد الآن مهما تطابقت المعاملات.
      **الوردة أُعيدت إلى بنائها السابق**: جُرّب بناء «Spiral Rose» (بتلات
      منتفخة على طول الحلزون) ثلاث مرات بمعاملات مختلفة، وبقيت البتلات
      ملتصقة بالحلزون لا تُقرأ كوردة. الأقل ادّعاءً أصدق من الأكثر تعقيداً.

- [x] **M24** — أدوات الأشكال في «الزخرفة والتكرار» (على نهج Shape Tools).
      **مستطيل** (نسبة + استدارة زوايا ⇒ يغطّي المستطيل المستدير) ·
      **بيضاوي** (نسبة ⇒ الدائرة عند 100%) · **مضلّع** (٣–٢٠ ضلعاً) ·
      **نجمة** (٣–٢٠ رأساً + نصف قطر داخلي) · **شعاع** · **خط**.
      كلها بارامترية ومساطرها تظهر مع شكلها وحده، وتتركّب مع الأوضاع الثلاثة.
      ٢١ شكلاً إجمالاً. النسبة عند 100% تعطي مربّعاً ودائرة مثاليين.

- [x] **M25** — لوحة التكرار بنسبة الورقة (بملاحظة صاحب المشروع: «المخرجات
      تبدو مربّعة»). كانت `viewBox="0 0 400 400"` مربّعة بينما مساحة الطباعة
      **186×253 مم** ⇒ ثلث الورقة يضيع. صارت `400×545` (نسبة 0.734 مقابل
      0.735 للورقة)، والشبكة تملأ الارتفاع الجديد بتباعد منتظم.
      مُتحقَّق: ٦ حالات (شعاعي · شبكي · مرآوي · ٤ و٨ زخارف · أقصى الحالات)
      → **1 صفحة** لكلٍّ · التسعة مجالات بلا انحدار.

- [x] **M26** — شبكات العلوم بنسبة الورقة أيضاً. المدى الرأسي صار أوسع
      من الأفقي بمعامل 1.36 ⇒ الشبكة تملأ A4 بدل ترك ثلثها.
      **انحدار وُجد وأُصلح:** شبكة الإحداثيات تُطبع مع قائمة النقاط تحتها،
      فبالارتفاع الكامل صارت ٣ صفحات في المستوى الصعب. سقفها الآن 188mm
      وراسم الدوال وحده يأخذ الارتفاع الكامل.
      مُتحقَّق: ١٥ حالة رسم دوال → **1 صفحة** · ٣ حالات إحداثيات → **2** ·
      التسعة مجالات بلا انحدار.

### قياس مقاس الورق — مُوثَّق
كل المخرجات **A4 بالضبط**: قِيست `MediaBox` في اثني عشر ملف PDF فكانت
`594.96 × 841.92 pt = 210 × 297 مم`، بمقاس واحد لا غير.
**لكن** ذلك لأن المتصفح احترم `@page{size:A4}`. جُرّب تحويله إلى `Letter`
فانكسرت ثلاثة مجالات: `teacher` 1→2 · `repeat` 1→2 · `math` 2→3.
السبب: ارتفاع Letter المطبوع 255 مم مقابل 273 مم لـA4.
**الخلاصة:** التصميم مبنيّ على A4 حصراً. من يطبع على Letter (أمريكا وكندا)
يحتاج إمّا اختيار A4 في حوار الطباعة، أو تخطيطاً بديلاً لم يُبنَ بعد.

### معلّق
- [ ] **دعم Letter** — تخطيط يتكيّف مع ارتفاع الورقة بدل mm ثابتة.
      يمسّ ورق المعلّم وشبكة التلوين وشبكة الرسم ولوحة التكرار.
- [ ] **مجال «تصاميم الحنّة» — موقوف بطلب صاحب المشروع.**
      الملفات موجودة (`assets/gen/henna.js` · `assets/page-henna.js` · `henna/`)
      لكنها **غير مرتبطة بالرئيسية ولا بـsitemap ولا تدخل الأرشيف**.
      أُنجزت سبعة أنماط (بوتيه · زهرة · دائرة · تعريشة · شريط · زاوية · قالب كفّ)
      وأُصلح فيها عطب تعبئة الدوائر، ولم تُستكمل المراجعة البصرية.
- [ ] ~~**مجال «تصاميم الحنّة»**~~ — مطلوب من صاحب المشروع. المفردات المِهندية
      خوارزمية بطبعها (بوتيه · زهور شعاعية · تعاريش · حدود منقّطة)،
      ومحرّكات الزخرفة الحالية تغطّي أغلبها. يحتاج بناءً مستقلاً.

- [ ] **الفنون: مناظر وتجريدي** — الخطوة التالية المقترحة: مناظر إجرائية
      (جبال · أشجار L-system · ماء) وفنّ تجريدي (حقول تدفّق · فورونوي).
      كلاهما مؤكّد الجودة ولا ينتظر أصولاً.
- [ ] ~~**الفنون: محتوى احترافي**~~ — طلب صاحب المشروع صور حيوانات ومناظر ولوحات
      واقعية وسريالية وتجريدية. **التجريدي والمناظر مولَّدة إجرائياً ممكنة
      وجيدة؛ الحيوان الواقعي لا يُولَّد بخوارزمية** ويحتاج أصولاً مرسومة.
      القرار معروض على صاحب المشروع.


- [x] **M25** — صفحات الثقة + التذييل الموحّد + og:image (2026-09-10).
      دافعه تقرير SEO خارجي: الموقع أقوى مرشّح لـ AdSense لكنه بلا صفحات ثقة.
      • `build.py` — مولّد صفحات ثابتة بلا اعتماديات (مكتبة بايثون القياسية فقط).
        المحتوى في `content/*.json` منفصل عن القالب ⇒ مدخل لوحة التحكم لاحقاً.
      • `about` · `privacy` · `terms` · `contact` — ٢١٧/٢١٩/١٦٢/١١٠ كلمة عربية.
        `privacy` تذكر Cloudflare Web Analytics وكوكيز AdSense صراحةً (شرط التقديم).
      • تذييل واحد يُحقن في كل صفحات الموقع من `build.py` — تعديله سطر واحد
        لا ٣٧ ملفاً. **لا تحرّر التذييل في HTML مباشرةً؛ يُدهس عند البناء.**
      • `og.png` (1200×630) + `og:url` + `twitter:card` في كل صفحة مفهرسة.
      • `assets/lang.js` — مبدّل اللغة مستخرَج من inline script في `index.html`.
      • `sitemap.xml` صار **مولَّداً** من الصفحات الموجودة فعلاً لا قائمة يدوية،
        و `henna` مستبعدة تلقائياً عبر `NOINDEX`.
      مُتحقَّق: عدّاد صفحات PDF للمجالات التسعة **مطابق للمرجع** (A4 594.96×841.92pt)،
      `scrollWidth == clientWidth == 360px`، صفر رابط داخلي مكسور، التذييل في ١٦/١٦ صفحة.
      أُصلح أثناءه: `007.gallery` كان يظهر «gallery.007» — انقلاب bidi لأن النص
      يبدأ برقم داخل فقرة RTL؛ الحل `dir="ltr"` على الرابط.

- [x] **M26** — ٢٠ صفحة ورقة ثابتة + فهرس `/worksheets/` (2026-09-10).
      المشكلة: المولّد يعمل بالجافاسكربت وجوجل لا ينفّذه ⇒ كان يرى موقعاً
      من عشر صفحات تسويقية بلا محتوى تعليمي واحد.
      • `core.js` → **2.4**: `fromURL(state, controls)` — روابط عميقة
        `/math/?skill=mul&count=40`. القيمة تُقبل **فقط إن كانت معلَنة في
        `cfg.controls`**؛ التحقق من النوع وحده لا يكفي (`skill=NOPE` يمرّ كنصّ
        وينتج ورقة فارغة). `range` يُقصّ إلى `[min,max]`. الأسبقية فوق localStorage
        — من ضغط رابط «ورقة ضرب» يريد الضرب لا اختيار الأسبوع الماضي.
      • `content/worksheets.json` — ٢٠ ورقة، ١٥٠–٢٢٣ كلمة عربية لكل صفحة،
        عبارات بحث حقيقية في `h1`. تغطية ٨ مجالات.
      • `tools/make-previews.py` — صور المعاينة **مولَّدة من المولّدات نفسها**:
        خادم محلي ← Chrome `--print-to-pdf` ← `sips` ← 620px. **الصورة ناتج
        حقيقي لا رسم يدوي**، فإن تغيّر المولّد أُعيد تشغيل السكربت. ٩٢٤ ك.ب لعشرين.
      • `LearningResource` + `BreadcrumbList` لكل ورقة، و `CollectionPage` للفهرس.
      • مسار الزحف: شريط في الرئيسية + رابط في التذييل — بدونهما الصفحات أيتام.
      مُتحقَّق:
      | الفحص | النتيجة |
      |---|---|
      | روابط داخلية | ٦٩٧ · **صفر مكسور** |
      | تفرّد صور المعاينة (md5) | **٢٠/٢٠** فريدة |
      | عدّاد صفحات PDF للمجالات التسعة | مطابق للمرجع — لا انحدار |
      | 360px على صفحة ورقة والفهرس | `scrollWidth == clientWidth` |
      | ضغط زرّ CTA فعلياً | `state` مطابق · ٥ chips مضغوطة · ورقة مرسومة |
      | رابط مشوّه `?skill=NOPE&evil=1&__proto__=x` | افتراضيات + ورقة سليمة · لا تلوّث prototype |
      `sitemap.xml`: 10 → 35 رابطاً.

- [x] **قياس الزيارات** — تبيّن أنه **مفعّل أصلاً** منذ ٢٠٢٦-٠٨-٢١ بوضع
      Automatic setup، ويسجّل فعلاً. **فخّ كاد يوقعنا:** `curl` العادي
      لا يرى البيكون فيبدو معطّلاً؛ Cloudflare يحقنه **على الحافة
      للمتصفحات الحقيقية فقط** — التحقق يحتاج `-A` بترويسة متصفح.
      **قرار: لا يُنشأ `content/site.json`.** آلية الحقن اليدوي في
      `build.py` تعمل لكن استعمالها الآن يضع بيكونين ⇒ احتساب مزدوج.
      تبقى للحالة الوحيدة التي تبرّرها: خروج الموقع من بروكسي Cloudflare.

- [x] **www.awraqna.com** (2026-09-10) — أُضيف كـ Custom domain على
      `awraqna-site`، سجلّ `CNAME www → awraqna-site.pages.dev`.
      مُتحقَّق: DNS يحلّ إلى `188.114.96.2 / .97.2`، و HTTPS **200**،
      و`canonical` في نسخة www يشير إلى الأصل بلا www في `/` و
      `/worksheets/` و`/articles/` ⇒ **لا ازدواج في الفهرس**.

- [ ] **Search Console** — خاصية `awraqna.com` (Domain) **مُنشأة وغير
      مُتحقَّقة**. `START VERIFICATION` يفتح نافذة موافقة Cloudflare كـ
      **popup خارج مجموعة تبويبات الوكيل** ⇒ هذه الخطوة بعينها غير قابلة
      للأتمتة، ينفّذها صاحب المشروع بثلاث ضغطات. ثم رفع `sitemap.xml`
      وطلب فهرسة `/` و`/worksheets/` و`/articles/`.
- [x] **M27** — نظام المقالات (2026-09-10).
      • `content/articles.json` + `article_html` / `articles_index_html`.
        `Article` (datePublished · wordCount · author) + `BreadcrumbList`،
        و `Blog` للفهرس، و `article:published_time`.
      • خمس مقالات ٥٧٧–٦٨٠ كلمة (٣٠٠٤ إجمالاً)، كلٌّ يربط داخلياً بصفحات
        الأوراق: **المقال يشرح والورقة تُنفّذ** — وهذا ما يربط المحتوى
        النصّي بالأداة بدل أن يكونا جزيرتين.
      • `read_ar()` — صياغة عربية سليمة للمدة (دقيقة/دقيقتين/دقائق/دقيقة).
      • `tools/i18n-harness.html` — **فاحص آلي** لفخّ تخصيص مبدّل اللغة:
        يفتح كل صفحة في iframe، يقلب `lang`، ويقارن `display` المحسوب.
      **درس معاد:** عادت اللغتان تظهران معاً في شريطي الرئيسية. السبب نفسه
      الذي أصاب `.badge-soon`: قاعدة مكوّن `.ws-promo-txt b{display:block}`
      تخصيصها (0,1,1) تتغلّب على `[data-en]{display:none}` وتخصيصها (0,1,0).
      **القاعدة المُلزِمة: لا تضع `display` في قاعدة مكوّن تطال عنصراً يحمل
      `data-ar`/`data-en`؛ أعطِ العنصر `.blk` بدلاً من ذلك.**
      مُتحقَّق (اختبار حساسية لا تشغيل أعمى): نظيف ٤٣/٤٣ → إعادة الخلل عمداً
      → الفاحص كشفه → استرجاع → نظيف ٤٣/٤٣. فاحص لا يستطيع الفشل لا يثبت شيئاً.
      ٩٤٥ رابطاً داخلياً · صفر مكسور · لا انحدار في الطباعة · 360px نظيف.
      `sitemap.xml`: 35 → 41 رابطاً.

- [ ] **مقالات إضافية** — البنية جاهزة؛ إضافة مقال = عنصر في
      `content/articles.json` ثم `python3 build.py`. مرشّحات: أنشطة رمضان،
      العودة للمدارس، عسر القراءة والخط، الألعاب الحسابية بلا أوراق.

- [x] **M28** — تجهيز النشر (2026-09-10).
      • `tools/preflight.py` — عشرة فحوص برمز خروج 0/1. كل فحص وُلد من عطل
        وقع فعلاً في هذا المشروع، لا من قائمة عامة.
      • `_headers`: `X-Robots-Tag: noindex` على `/*.md` و`/*.py` و`/tools/*`
        و`/content/*` و`/henna/*`. **السبب:** Cloudflare Pages ينشر كل ما في
        المستودع ولا يملك ملف استثناء، و`robots.txt` **يطلب** بينما الترويسة
        **تُلزم**. الاثنان معاً؛ لا تحذف أيّهما.
      • `DEPLOY.md` أُعيدت كتابته — كان مكتوباً لتدفّق الرفع اليدوي بقائمة
        «ما لا يُرفع»، وهي غير قابلة للتطبيق في النشر من git.
      **عيب اكتُشف في الفاحص نفسه:** الفحص ١ يشغّل `build.py` فيُصلح أعطال
      الصفحات المولَّدة قبل أن تراها الفحوص ٢–٥. صحيح عملياً (النشر يسبقه
      بناء دائماً) لكنه كان صامتاً ⇒ وُثّق في الكود وفي مخرجات الفاحص.
      مُتحقَّق باختبار حساسية: رابط مكسور ✗ · canonical خاطئ ✗ · إزالة
      Disallow ✗ · بعد الاسترجاع ✓. وصيغة `_headers`: ١٣ قاعدة صفر خطأ.

- [x] **الرفع إلى GitHub** (2026-09-10، بإذن صريح من صاحب المشروع).
      ثمانية commits ⇒ `c0ba529..81f528a`. مُتحقَّق: ١٢٢ ملفاً على البعيد،
      و`origin/main..HEAD` = 0، ووجود `worksheets/` و`articles/` و`about/`
      و`assets/previews/` مؤكَّد بـ `git cat-file -e`.
      **عطل واجهناه:** `HTTP 400 RPC failed` — سببه `http.postBuffer`
      الافتراضي (١ ميجا) وحزمة أكبر منه بسبب صور المعاينة. الحل
      `git config http.postBuffer 524288000`. **الإعداد محليّ في
      `.git/config` ⇒ يلزم تكراره على أي جهاز جديد.** ورسالة
      `Everything up-to-date` بعد الخطأ **مضلّلة** — لا شيء رُفع.

- [ ] **النشر** — مشروع Cloudflare Pages باسم `awraqna-site` (اسم
      `awraqna` محجوز بمشروع الرفع اليدوي القديم)، ثم نقل النطاق وحذف
      القديم. **يحتاج حساب صاحب المشروع ⇒ ينفّذه هو، لا الوكيل.**
      الخطوات كاملة في `DEPLOY.md`.
      نجاح: `curl -sI https://awraqna.com | head -1` → `HTTP/2 200`
- [ ] **مراجعة 007** — `007-gallery` ليس مستودع git، وتعديل `ads.js` فيه بلا شبكة أمان.
      الفرق الوحيد هو إضافة كتلة `awraqna` إلى `HOUSE[]`؛ لا شيء حُذف.

### أيتام معروفة
- **الفضاء الضيّق:** ٩ تركيبات (ضرب/طرح · صفوف دنيا · سهل) فضاؤها العددي أصغر من ٤٠ سؤالاً
  فعلياً ⇒ `generate` يسمح بالتكرار كملاذ أخير ويسجّل `console.warn`.
  هذا سلوك صحيح لا عيب: ورقة ناقصة أسوأ من ورقة بها تكرار. لا تُصلحه بتوسيع المدى
  فتكسر ملاءمة الصف.
- `assets/ads.js`: روابط `kashf` و `quran` و `seven` ما زالت `#` (موروثة من 007، خارج نطاقنا).
- `assets/brand.css` منسوخ بألوان 007 الذهبية — قد تُعدَّل هوية أوراقنا لاحقاً؛
  لا تُحذف متغيرات CSS منه، `ads.js` يعتمد على `--line` و `--bg-2`.
